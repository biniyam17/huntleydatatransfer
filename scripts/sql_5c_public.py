select_all_users = (
"SELECT * FROM users"
)
select_all_students = (
"SELECT * FROM students"
)
select_all_renters = (
"SELECT * FROM renters"
)
select_all_listings = (
"SELECT * from listings"
)
select_all_rented_listings = (
"SELECT * from rented_listings"
)

select_user_by_id = (
"SELECT email FROM users WHERE id = %s"
)

select_account_notes = (
"SELECT * FROM account_notes"
)
